// Used in the Simplifience logo and the post titles.
$uncomfortable = 'Revalia', serif

// Used in the dictionary definition on the home page.
$exciting = 'Niconne', serif

// The main text.
$boring = 'Noto Serif', arial, sans-serif

$text-size = 14px
$text-color = black
$background-color = white
$line-size = 1.5em
$line-margin = $line-size * .5
$main-header-size = $text-size * 3
$content-header-size = $text-size * 2
$commentary-padding = ($line-size / 2) $line-size
$border-width = 1px
