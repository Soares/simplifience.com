// Generated by CoffeeScript 1.4.0
(function() {
  var arrow, backArrow, height, numberTick, numberWidth, padding, width, zeroTick;

  width = 500;

  height = 50;

  padding = 50;

  numberTick = 35;

  numberWidth = width - (3 * padding) - numberTick;

  arrow = function(paper, x, y) {
    var axis, dx, dy, ex, ey, path;
    dx = 8;
    dy = 2;
    ex = x + numberTick;
    ey = y + 0.5;
    axis = paper.path("M" + x + " " + ey + "l" + (numberTick - dx) + " 0");
    $(axis.node).attr('class', 'axis-line');
    path = paper.path("M" + ex + " " + ey + " " + (ex - dx) + " " + (ey - dy) + " " + (ex - dx) + " " + (ey + dy) + "z");
    return $(path.node).attr('class', 'axis-arrow');
  };

  backArrow = function(paper, x, y) {
    var axis, dx, dy, path, sx, sy;
    dx = 8;
    dy = 2;
    sx = x;
    sy = y + 0.5;
    axis = paper.path("M" + (x + dx) + " " + sy + "l" + (numberTick - dx) + " 0");
    $(axis.node).attr('class', 'axis-line');
    path = paper.path("M" + sx + " " + sy + " " + (sx + dx) + " " + (sy + dy) + " " + (sx + dx) + " " + (sy - dy) + "z");
    return $(path.node).attr('class', 'axis-arrow');
  };

  zeroTick = function(paper, x, y) {
    var tick;
    tick = paper.path("M" + (x + 0.5) + " " + (y + 0.5) + "l0 -4");
    return $(tick.node).attr('class', 'axis-line');
  };

  $(function() {
    return $('.number-line').each(function() {
      var paper;
      paper = util.paperFor(this, width, height);
      Raphael.g.axis(padding + numberTick, height / 2 - 5, numberWidth, -5, 5, 10, null, 0, 't', null, paper);
      arrow(paper, padding + numberTick + numberWidth, height / 2 - 5);
      backArrow(paper, padding, height / 2 - 5);
      return zeroTick(paper, padding + numberTick + numberWidth / 2, height / 2 - 5);
    });
  });

}).call(this);
